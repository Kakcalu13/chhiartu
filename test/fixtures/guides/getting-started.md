# Getting started

Exists so the fixture tree has a nested folder to titleize into "Guides", and a
relative link target for `demo.md` to point at.

## Opening documents

Drop a Markdown file on the window, or press ⌘O. The folder containing that file
becomes the workspace root.
